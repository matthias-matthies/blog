const LayoutHeader = ({ className = "" }: {className?: string;}) => {
    return (
        <header className={className}>
            Navbar hier
        </header>
    )
}

export default LayoutHeader
