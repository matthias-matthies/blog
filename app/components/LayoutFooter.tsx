const LayoutFooter = ({ className = "" }: {className?: string;}) => {
    return (
        <footer className={className}>
            Legals hier
        </footer>
    )
}

export default LayoutFooter
